<div id="finder">
  <div id="app-view"></div>
</div>


<% content_for(:js) do %>
  var county = {fips_code: '<%= params[:fips_code].to_s %>', full_name: 'Allegheny County'};
  var acaPlans = new HealthPGH.Collections.AcaPlans([]);
  acaPlans.reset( <%== @plans.to_json %> );

  var router = new HealthPGH.Routers.AcaPlanSearch({
    county: county,
    plans: acaPlans
  });
<% end %>
